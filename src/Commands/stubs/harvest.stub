<?php
{{ namespace }}
use Goldfinch\Harvest\Harvest;

class {{ class }}Harvest extends Harvest
{
    public function run(): void
    {
        // .. SilverStripe\Security\Member::mill()->count(3)->make();
    }
}
